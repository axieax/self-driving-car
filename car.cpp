#include <iostream>
#include "tesla.h"

int main() {
  // heard C++ gud for beep boop
  if (!car_move()) car_move();
  // should be gud enough i dont see how it can be improved
  // feel free to send in some pull requests but i dont think it can beat this
  if (going_to_crash()) dont();
  if (car_crash()) die();
  // get out me mom's car vroom vroom
  for (;;) std::cout << "Vroom Vroom" << std::endl;
}
